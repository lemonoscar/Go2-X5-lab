# P4: Flat Arm Unlock On Top Of P1

## 1. 定位

这个 `P4` 不是 `train_route/step_04_arm_fixed_base_imitation.md` 里那条固定底座 imitation-only 分支。

这里的 `P4` 是当前 `Go2-X5` route 的实用延续版本：

- 起点：`P1` 的 flat foundation checkpoint
- 目标：在 **平地** 上把机械臂从“锁死默认位姿”过渡到“可控解锁”
- 约束：保持整套 policy 的输入输出维度不变，保证可以直接 `resume`

对应新任务：

- `RobotLab-Isaac-Velocity-Flat-Go2-X5-ArmUnlock-v0`

## 2. 续训模型

直接续训用这个 checkpoint：

- `logs/rsl_rl/go2_x5_foundation_flat/2026-03-12_12-11-27/model_8000_armdims.pt`

如果手里只有旧的 `model_8000.pt`，要先迁移成 `model_8000_armdims.pt` 再训。

## 3. 输入输出维度

`P1` 和 `P4` 保持完全一致的 route 接口：

- actor obs: `259`
- critic obs: `259`
- action: `18`

分解如下：

- `base_lin_vel`: `3`
- `base_ang_vel`: `3`
- `projected_gravity`: `3`
- `velocity_commands`: `3`
- `joint_pos`: `18`
- `joint_vel`: `18`
- `last_action`: `18`
- `height_scan`: `187`
- `arm_joint_command`: `6`

所以 `P1 -> P4` 可以直接续训，不需要改网络首尾层。

## 4. P4 环境语义

`P4` 的核心不是加 rough，而是先在 flat 上把 arm 问题单独解决掉。

- terrain: flat
- `num_envs = 2048`
- standing ratio: `1.0`
- base command:
  - `lin_vel_x = (0.0, 0.0)`
  - `lin_vel_y = (0.0, 0.0)`
  - `ang_vel_z = (0.0, 0.0)`
- base command 不再继续 curriculum 放大
- arm command 使用 `use_default_offset=True`
- 当前默认 arm 姿态按仓库里的最新资产设置，基准是 `0 0 0 0 0 0`
- 训练目标更偏向 `stand-still arm control`，而不是低速 locomotion

## 5. Arm 解锁流程

`P4` 里不是直接把 arm 命令一次性开到大范围，而是做线性 range curriculum。

初始 arm command range:

- `joint1: (-0.25, 0.25)`
- `joint2: (0.00, 0.45)`
- `joint3: (0.00, 0.45)`
- `joint4: (-0.25, 0.25)`
- `joint5: (-0.20, 0.20)`
- `joint6: (-0.20, 0.20)`

最终 arm command range:

- `joint1: (-2.40, 3.00)`
- `joint2: (0.00, 3.00)`
- `joint3: (0.00, 3.00)`
- `joint4: (-1.45, 1.45)`
- `joint5: (-1.45, 1.45)`
- `joint6: (-1.45, 1.45)`

range curriculum:

- `curriculum_iterations = 128`
- linearly interpolate from the initial range to the final range

这组范围接近 URDF 物理限位，但留了一点安全边界：

- `joint1` URDF: `[-2.618, 3.14]`
- `joint2` URDF: `[0, 3.14]`
- `joint3` URDF: `[0, 3.14]`
- `joint4/5/6` URDF: `[-1.5708, 1.5708]`

所以要特别注意：

- `joint2` 和 `joint3` 不能给负数
- `joint1/4/5/6` 可以有负数

arm action scale 也同步放大到：

- `joint1: 1.20`
- `joint2: 1.20`
- `joint3: 1.20`
- `joint4: 0.80`
- `joint5: 0.70`
- `joint6: 0.70`

## 6. 奖励函数

`P4` 使用 reward curriculum，从 `P1` 权重平滑过渡到 `P4 target`，避免从锁臂 checkpoint 直接跳到开臂任务。

reward curriculum:

- `curriculum_iterations = 128`

### 6.1 P4 target reward weights

- `track_lin_vel_xy_exp = 4.0`
- `track_ang_vel_z_exp = 2.0`
- `lin_vel_z_l2 = -1.8`
- `ang_vel_xy_l2 = -0.12`
- `flat_orientation_l2 = -0.60`
- `base_height_l2 = -0.2`
- `body_lin_acc_l2 = -0.020`
- `joint_torques_l2 = -1.5e-5`
- `joint_acc_l2 = -1.0e-7`
- `joint_pos_limits = -2.0`
- `joint_power = -1.0e-5`
- `stand_still = -4.0`
- `joint_pos_penalty = -1.2`
- `action_rate_l2 = -0.01`
- `undesired_contacts = -1.0`
- `contact_forces = -1.0e-4`
- `feet_air_time = 0.0`
- `feet_air_time_variance = 0.0`
- `feet_contact_without_cmd = 0.4`
- `feet_slide = -0.15`
- `feet_gait = 0.0`
- `arm_joint_pos_tracking_l2 = -3.5`
- `arm_joint_vel_l2 = -0.001`
- `arm_joint_acc_l2 = -7.5e-7`
- `arm_joint_torques_l2 = -6.0e-5`
- `arm_action_rate_l2 = -0.012`
- `arm_joint_pos_limits = -2.0`
- `arm_joint_deviation_l2 = 0.0`
- `arm_motion_tilt_penalty = -0.35`
- `arm_action_in_unstable_base = -0.12`
- `arm_stable_track_bonus = 1e-6`

### 6.2 为什么这样设

- `arm_joint_deviation_l2` 直接放到 `0.0`，避免 arm 在大范围跟踪时还被默认位姿强拉回去。
- `arm_joint_pos_tracking_l2` 保留中等偏强强度，确保 arm 真正跟 command 走，不会只顾着“僵住”。
- `arm_motion_tilt_penalty` 和 `arm_action_in_unstable_base` 明确约束“arm 不能把底盘打崩”。
- `stand_still / joint_pos_penalty / flat_orientation / ang_vel_xy / body_lin_acc` 全部加重，目标是 arm 动的时候底盘仍然稳定站立。
- `feet_air_time / feet_gait` 归零，避免 P4 继续鼓励步态行为。
- `arm_stable_track_bonus` 现在基本关闭，因为当前实现的 gate 还是按绝对目标范数，不适合在 `use_default_offset=True` 下强开。

## 7. 域随机化和 sim2sim

`P4` 是 flat 阶段，但当前版本把随机化刻意收小，重点观察“动臂本身是否会破坏 stand-still 稳定性”。

- friction:
  - `static = (0.65, 1.00)`
  - `dynamic = (0.55, 0.90)`
  - `restitution = (0.0, 0.10)`
- base mass scale: `(0.96, 1.04)`
- other body mass scale: `(0.98, 1.03)`
- base COM perturbation: `+-0.01 m`
- actuator gain interval drift:
  - interval `3.0 ~ 5.0 s`
  - stiffness `(0.95, 1.05)`
  - damping `(0.95, 1.05)`
- external force/torque: disabled
- push: disabled
- obs noise:
  - `base_ang_vel: [-0.015, 0.015]`
  - `projected_gravity: [-0.02, 0.02]`
- action delay:
  - `delay_range = (0, 0)`
  - `hold_prob = 0.02`
  - `noise_std = 0.003`
- observation delay: `0` step

## 8. PPO 超参数

RSL-RL runner:

- `num_steps_per_env = 32`
- `max_iterations = 5000`
- `save_interval = 250`
- `experiment_name = go2_x5_arm_unlock_flat`
- actor hidden dims: `[512, 256, 128]`
- critic hidden dims: `[512, 256, 128]`
- activation: `elu`
- `learning_rate = 5e-4`
- `entropy_coef = 0.003`
- `gamma = 0.99`
- `lam = 0.95`
- `num_learning_epochs = 5`
- `num_mini_batches = 4`
- `desired_kl = 0.01`

## 9. 服务器运行

如果服务器上已经有 `model_8000_armdims.pt`，直接：

```bash
cd /path/to/Go2-X5-lab
source /path/to/miniconda3/etc/profile.d/conda.sh
conda activate isaaclab
python -m pip install -e source/robot_lab

python scripts/reinforcement_learning/rsl_rl/train.py \
  --task=RobotLab-Isaac-Velocity-Flat-Go2-X5-ArmUnlock-v0 \
  --headless \
  --num_envs=2048 \
  --resume \
  --no_load_optimizer \
  --checkpoint=logs/rsl_rl/go2_x5_foundation_flat/2026-03-12_12-11-27/model_8000_armdims.pt
```

如果只有旧 checkpoint：

```bash
python scripts/tools/migrate_go2_x5_route_checkpoint.py \
  --input logs/rsl_rl/go2_x5_foundation_flat/2026-03-12_12-11-27/model_8000.pt \
  --output logs/rsl_rl/go2_x5_foundation_flat/2026-03-12_12-11-27/model_8000_armdims.pt
```

## 10. 训练中的检查点

建议重点看 4 个现象：

- arm command 非零时，`arm_joint_pos_tracking_l2` 是否持续下降
- `flat_orientation_l2` 和 `lin_vel_z_l2` 是否明显恶化
- standing 环境里是否出现 arm 高频抖动
- base 有轻微移动命令时，arm 是否还能跟踪

如果 `1000 ~ 1500` 轮内 arm 仍然几乎不动，优先检查：

- 默认姿态是不是你期望的全零姿态
- arm command range curriculum 是否生效
- checkpoint 用的是不是 `model_8000_armdims.pt`

## 11. P4 结束后的去向

`P4` 结束后有两条合理路线：

- 去 `P3` 风格的 rough arm 阶段：把 flat 上学到的 arm/base 协调迁到 rough
- 去严格的 `train_route step_04/05` 分支：单独做 fixed-base 或 standing-base manipulation

如果你当前最关心的是“机器人在平地上先把 arm 用起来”，那先做这个 `P4` 是对的。
